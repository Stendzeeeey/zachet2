2.	Реализовать работу со связным списком на языке Python. Создать два класса: Node – элемент списка с атрибутами data (данные) и 
next (адрес следующего элемента списка), а также класс List (список)  c атрибутами head  - адрес первого элемента списка (первоначальное значение None) 
и  length – длина списка (его необходимо изменять при каждом добавлении и удалении элементов). Методы: append – добавить элемент в конец списка, 
remove – удалить элемент с начала списка, out – вывести список на экран, len – вывести длину списка. 
Реализовать меню пользователя для работы со списком.

def my_main_function():
    class Node:
        def __init__(self, data):
            self.data = data
            self.next = None


    class List:
        def __init__(self):
            self.head = None
            self.length = 0

        def append(self, data):
            node = Node(data)
            if not self.head:
                self.head = node
            else:
                current = self.head
                while current.next:
                    current = current.next
                current.next = node
            self.length += 1

        def remove(self):
            if not self.head:
                return None
            else:
                data = self.head.data
                self.head = self.head.next
                self.length -= 1
                return data

        def out(self):
            if not self.head:
                print("Список пуст")
            else:
                current = self.head
                while current:
                    print(current.data, end=" ")
                    current = current.next
                print()

        def __len__(self):
            return self.length


    linked_list = List()
    while True:
        print("1. Добавить элемент")
        print("2. Удалить элемент")
        print("3. Вывести список")
        print("4. Вывести длину списка")
        print("5. Выход")
        choice = input("Введите номер операции: ")
        if choice == "1":
            data = input("Введите данные элемента: ")
            linked_list.append(data)
        elif choice == "2":
            data = linked_list.remove()
            if data is not None:
                print(f"Удален элемент со значением {data}")
            else:
                print("Список пуст")
        elif choice == "3":
            linked_list.out()
        elif choice == "4":
            print(f"Длина списка: {len(linked_list)}")
        elif choice == "5":
            break
        else:
            print("Неверный выбор")

my_main_function() 
